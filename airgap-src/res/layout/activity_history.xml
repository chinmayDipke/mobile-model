<?xml version="1.0" encoding="utf-8"?>
<!--
  Same shape as the main screen: big title, labelled sections, rows separated by
  hairlines rather than a card drawn around every single item. Cards inside
  cards is what made the old version look generic.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg"
    android:fillViewport="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="40dp">

        <TextView
            android:id="@+id/historyCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginTop="48dp"
            android:letterSpacing="-0.02"
            android:textColor="@color/text_primary"
            android:textSize="32sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/historySub"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="24dp"
            android:lineSpacingMultiplier="1.35"
            android:textColor="@color/ok"
            android:textSize="15sp" />

        <TextView
            style="@style/SectionLabel"
            android:text="WHAT AIRGAP READ" />

        <LinearLayout
            android:id="@+id/statsBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="20dp"
            android:background="@drawable/bg_surface"
            android:orientation="vertical"
            android:paddingStart="18dp"
            android:paddingTop="14dp"
            android:paddingEnd="18dp"
            android:paddingBottom="16dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginTop="12dp"
            android:layout_marginEnd="24dp"
            android:lineSpacingMultiplier="1.35"
            android:text="Something has to look in order to decide. For an ignored message that is a short check which asks one question and keeps nothing — no model, no storage, no log, no network. Only the count survives."
            android:textColor="@color/text_tertiary"
            android:textSize="13sp" />

        <TextView
            style="@style/SectionLabel"
            android:text="BLOCKED" />

        <LinearLayout
            android:id="@+id/historyList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:orientation="vertical" />

        <LinearLayout
            android:id="@+id/historyClear"
            style="@style/Row"
            android:layout_marginTop="16dp">

            <TextView
                style="@style/RowLabel"
                android:text="Clear history"
                android:textColor="@color/text_tertiary" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>
